#include <stdio.h>

int main () {
    float gpa = 3.05;
    float price = 19.99;
    float temperature = -10.1;
    printf("My current %f\n", gpa);
    printf("The price is $%f\n", price);
    printf("The temperature is %f F\n", temperature);
}