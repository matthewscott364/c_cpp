#include <stdio.h>

int main() {
    int x = 10;
    int y = 2;
    int z = 0;

    // z = x + y;
    // z = x - y;
    // z = x * y;
    // z = x / y;
    // z = x % y;

    x+=3;

    printf("%d", x);

    return 0;
}