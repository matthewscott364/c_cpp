#include <stdio.h>

// For Loop = Repeat some code a limited # of times
// for(Initialization; Condition; Update)

int main() {
    for(int i = 10; i >= 0; i--) {
        printf("%d\n", i);
    }
    return 0;
};