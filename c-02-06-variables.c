#include <stdio.h>
#include <stdbool.h>

// If & else statements and how they are used with booleans.

int main () {
    bool isStudent = true;
    if(isStudent){
        printf("You are a student.");
    }
    else{
        printf("You are not a student.");
    }
}