#include <stdio.h>

void checkBalance(float balance);
float deposit();
float withdraw(float balance);

int main() {
    return 0;
}

void checkBalance(float balance) {
}
float deposit() {
    return 0.0f;
}
float withdraw(float balance) {
    return 0.0f;
}